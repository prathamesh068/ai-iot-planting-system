# ── Raspberry Pi ─────────────────────────────────────────────────────────────
# Before running pip install, install system packages:
#
#   sudo apt update && sudo apt install -y \
#     python3-dev python3-pip python3-venv \
#     libgpiod-dev swig libatlas-base-dev libopencv-dev
#
# Then: pip install -r requirements.pi.txt

# Adafruit / GPIO (RPi-specific packages included)
Adafruit-Blinka==8.69.0
adafruit-circuitpython-dht==4.0.10
RPi.GPIO==0.7.1
rpi-lgpio==0.6

# Google APIs
google-generativeai==0.8.6
google-api-python-client==2.190.0
google-auth==2.48.0
google-auth-oauthlib==1.2.4

# Computer Vision
opencv-python==4.13.0.92

# Numerical
numpy==2.4.2

# Environment
python-dotenv==1.2.1
